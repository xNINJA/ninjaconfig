exec clear

exec crosshairswitcher/switcher; medic

alias fix fixmedic

bind "alt" radarcontrol
bind "e" v_help
bind "4" +panic
bind "5" +attack3

// Removes chat spam on using Über (uncomment for it to work); must remain on the bottom
//exec noteamcall