exec clear

exec crosshairswitcher/switcher; spy

alias fix fixspy

bind "alt" zoomcontrol
bind "mouse2" +watch
bind "4" equip_disguisePDA
bind "5" spy_undisguise

bind f1 disguise_group1
bind f2 disguise_group2
bind f3 disguise_group3

// Removes chat spam on sapper (uncomment for it to work); must remain on the bottom
//exec noteamcall